# Coding Standards

- Strict TypeScript
- No `any`
- Prefer Signals over RxJS (except streaming)
- No business logic inside UI components
- All API calls via services
- Use OnPush Change Detection
- Feature-based folder structure
- No cross-feature coupling
